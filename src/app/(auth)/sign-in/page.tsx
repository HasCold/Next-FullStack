// (auth) -->> this is the bundle or the route which not actually consider but for the testing purposes. 

import React from "react";

const page = () => {
	return (
		<div>Page</div>
	)
}

export default page;